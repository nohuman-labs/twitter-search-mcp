apiVersion: v1
kind: Service
metadata:
  name: twitter-search-mcp
  labels:
    app.kubernetes.io/name: twitter-search-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: twitter-search-mcp
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
